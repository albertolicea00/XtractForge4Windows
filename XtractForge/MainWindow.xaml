<Window
    x:Class="XtractForge.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:XtractForge.Views"
    Title="XtractForge">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Custom title bar drag region -->
        <Grid x:Name="TitleBarArea" Grid.Row="0" Height="32">
            <StackPanel Orientation="Horizontal" Spacing="8" Margin="12,0,0,0"
                        VerticalAlignment="Center">
                <FontIcon Glyph="&#xE896;" FontSize="14" />
                <TextBlock Text="XtractForge"
                           Style="{StaticResource CaptionTextBlockStyle}"
                           VerticalAlignment="Center" />
            </StackPanel>
        </Grid>

        <MenuBar Grid.Row="1" Margin="4,0,0,0">
            <MenuBarItem Title="File">
                <MenuFlyoutItem Text="Paste URL" Click="OnPasteUrl">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Modifiers="Control" Key="V"
                                             Invoked="OnPasteAccelerator" />
                    </MenuFlyoutItem.KeyboardAccelerators>
                    <MenuFlyoutItem.Icon>
                        <FontIcon Glyph="&#xE77F;" />
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <MenuFlyoutItem Text="Open Downloads Folder" Click="OnOpenDownloadsFolder">
                    <MenuFlyoutItem.Icon>
                        <FontIcon Glyph="&#xE8B7;" />
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <MenuFlyoutSeparator />
                <MenuFlyoutItem Text="Settings" Click="OnOpenSettings">
                    <MenuFlyoutItem.Icon>
                        <FontIcon Glyph="&#xE713;" />
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
                <MenuFlyoutSeparator />
                <MenuFlyoutItem Text="Exit" Click="OnExit" />
            </MenuBarItem>
            <MenuBarItem Title="View">
                <RadioMenuFlyoutItem x:Name="ThemeSystem" Text="Follow System"
                                     GroupName="Theme" Click="OnThemeChanged" />
                <RadioMenuFlyoutItem x:Name="ThemeLight" Text="Light"
                                     GroupName="Theme" Click="OnThemeChanged" />
                <RadioMenuFlyoutItem x:Name="ThemeDark" Text="Dark"
                                     GroupName="Theme" Click="OnThemeChanged" />
            </MenuBarItem>
            <MenuBarItem Title="Help">
                <MenuFlyoutItem Text="About XtractForge" Click="OnAbout" />
            </MenuBarItem>
        </MenuBar>

        <views:MainPage x:Name="Main" Grid.Row="2" />
    </Grid>
</Window>
